# src/config/api_urls.py
from django.urls import path, include

urlpatterns = [
    # zatím jen placeholder – později sem dáme routery jednotlivých apps
    # path("auth/", include("accounts.api.urls")),
]
